<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>
    <h2>Upcoming Service Projects</h2>
    <ul>
        <% projects.forEach(project => { %>
            <li><strong><%= project.name %></strong>: <%= project.description %>: <date><%= project.date %>.toLocaleDateString() %></date></li>
        <% }); %>
    </ul>
</main>
<%- include('partials/footer') %>